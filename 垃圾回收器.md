# 垃圾回收器(Garbage Collector)

## 什么是垃圾回收器

垃圾回收器(GC)是自动内存管理系统的一部分,负责识别和回收程序中不再使用的内存对象,防止内存泄漏并优化内存使用。

## 工作原理

### 1. 标记-清除(Mark-Sweep)
- **标记阶段**: 从根对象开始遍历,标记所有可达对象
- **清除阶段**: 回收所有未被标记的对象

### 2. 复制算法(Copying)
- 将内存分为两个区域
- 将存活对象从一个区域复制到另一个区域
- 清空原区域

### 3. 标记-整理(Mark-Compact)
- 标记存活对象
- 将存活对象向内存一端移动
- 清理边界外的内存

## Java中的垃圾回收器

### Serial GC
- 单线程回收
- 适用于小型应用
- 启动参数: `-XX:+UseSerialGC`

### Parallel GC
- 多线程并行回收
- 注重吞吐量
- 启动参数: `-XX:+UseParallelGC`

### CMS (Concurrent Mark Sweep)
- 并发标记清除
- 减少停顿时间
- 启动参数: `-XX:+UseConcMarkSweepGC`

### G1 GC (Garbage First)
- 分区域回收
- 可预测的停顿时间
- Java 9+默认GC
- 启动参数: `-XX:+UseG1GC`

### ZGC
- 低延迟垃圾回收器
- 停顿时间<10ms
- 适用于大堆内存
- 启动参数: `-XX:+UseZGC`

### Shenandoah GC
- 并发整理
- 极低停顿时间
- 启动参数: `-XX:+UseShenandoahGC`

## 内存分代

### 年轻代(Young Generation)
- **Eden区**: 新对象分配区域
- **Survivor区**: 存放经过一次GC后存活的对象
- 采用复制算法

### 老年代(Old Generation)
- 存放长期存活的对象
- 采用标记-清除或标记-整理算法

### 永久代/元空间(Metaspace)
- 存储类元数据
- Java 8后使用元空间替代永久代

## GC触发条件

1. Eden区满时触发Minor GC
2. 老年代空间不足触发Major GC/Full GC
3. 显式调用`System.gc()`(不推荐)
4. 方法区空间不足

## 性能调优参数

```bash
# 设置堆大小
-Xms2g -Xmx2g

# 设置年轻代大小
-Xmn1g

# 设置Survivor区比例
-XX:SurvivorRatio=8

# 设置晋升阈值
-XX:MaxTenuringThreshold=15

# 打印GC日志
-XX:+PrintGCDetails -XX:+PrintGCDateStamps

# GC日志文件
-Xloggc:gc.log
```

## 最佳实践

1. **选择合适的GC**: 根据应用特点选择
2. **监控GC行为**: 使用工具分析GC日志
3. **避免频繁Full GC**: 优化对象生命周期
4. **合理设置堆大小**: 避免过大或过小
5. **减少大对象创建**: 避免直接进入老年代

## 常用监控工具

- `jstat`: JVM统计监控工具
- `jmap`: 内存映射工具
- `jconsole`: JVM监控控制台
- `VisualVM`: 可视化监控工具
- `GCViewer`: GC日志分析工具

## 参考资源

- [Oracle Java GC调优指南](https://docs.oracle.com/javase/8/docs/technotes/guides/vm/gctuning/)
- [深入理解Java虚拟机](https://book.douban.com/subject/34907497/)
